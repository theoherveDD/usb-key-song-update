# USB Key Song Update - Environment Configuration

# === APPLICATION ===
NODE_ENV=development
PORT=3000

# === PATHS ===
# Where your music hard drive is mounted
HARD_DRIVE_PATH=/Volumes/MusicDrive

# Where your DJ USB key is mounted
USB_DRIVE_PATH=/Volumes/DJ_USB

# Root directory for downloaded music
DOWNLOAD_BASE_PATH=/Volumes/MusicDrive/DJ_Music

# Rekordbox installation/library path
REKORDBOX_DB_PATH=/Applications/Pioneer/rekordbox

# === TOOLS ===
# Path to scdl executable (SoundCloud downloader)
SCDL_PATH=/usr/local/bin/scdl

# Path to beatport-dl executable (Beatport downloader)
BEATPORT_DL_PATH=/usr/local/bin/beatport-dl

# === SOUNDCLOUD ===
# Optional: Comma-separated list of SoundCloud usernames to download reposts from
# Example: SOUNDCLOUD_FOLLOWED_USERS=username1,username2,username3
SOUNDCLOUD_FOLLOWED_USERS=

# === SPOTIFY API ===
# Spotify API credentials (get from https://developer.spotify.com/dashboard)
# 🆕 NEW OAUTH 2.0: Just add Client ID & Secret below, then click "Connect Spotify" in the web UI!
# No need to manually set SPOTIFY_REFRESH_TOKEN anymore - it will be saved automatically
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/spotify/callback
# SPOTIFY_REFRESH_TOKEN=  # ⚠️ DEPRECATED: Set automatically via OAuth flow, don't edit manually

# === TIDAL API ===
# Tidal API credentials (if available - Tidal API is not publicly available)
TIDAL_CLIENT_ID=
TIDAL_CLIENT_SECRET=

# === BEATPORT ===
# Beatport account credentials for downloading
BEATPORT_USERNAME=
BEATPORT_PASSWORD=

# === MONITORING ===
# How often to check for new downloads (in minutes)
CHECK_INTERVAL_MINUTES=30

# Enable automatic sync when drives are connected
AUTO_SYNC_ENABLED=true

# === LOGGING ===
# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# Log file path
LOG_FILE=logs/app.log
